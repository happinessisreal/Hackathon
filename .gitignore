.venv/
venv/
__pycache__/
*.pyc
.env
data/*.db
data/*.db-wal
data/*.db-shm
backups/*.db
.pytest_cache/
*.egg-info/
node_modules/
.DS_Store
.playwright-mcp/

# The organizers' case brief - their material, not our deliverable; kept
# locally for reference but never committed to the public submission repo.
RoboFusion_1.0_SCS-RG_Round1_Case.pdf

# Video recordings — submitted via Google Drive per the case's submission
# procedure, never committed. A single take runs to hundreds of MB and this
# is a public repo.
Video/
*.mp4
*.mkv
*.mov
