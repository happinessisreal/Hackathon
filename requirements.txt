fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0.30,<3.0
aiosqlite>=0.20,<1.0
pydantic>=2.7,<3.0
python-dotenv>=1.0,<2.0
websockets>=12.0,<13.0
httpx>=0.27,<1.0
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<1.0

# Bonus 3 (ML risk prediction): needed only to RE-TRAIN via ml/train.py.
# The backend serves the shipped ml/model.json artifact in pure Python -
# no sklearn/numpy import anywhere in backend/.
scikit-learn>=1.4,<2.0

# Bonus 1 (camera occupancy): OPTIONAL - only for sim/camera_node.py's
# --webcam/--video modes. Its --synthetic mode needs nothing extra.
# opencv-python>=4.9,<5.0
